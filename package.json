{
  "name": "panda",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "parcel src/index.html --no-cache",
    "build": "rm -rf dist && parcel build src/index.html --public-url ./"
  }
}
